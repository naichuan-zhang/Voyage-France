@model IEnumerable<WebApplication6.Models.Image>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScenicSpot.ScenicSpotName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScenicSpot.ScenicSpotName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImageFilename }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImageFilename }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImageFilename })
        </td>
    </tr>
}

</table>
